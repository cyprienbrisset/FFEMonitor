# EngageWatch - Configuration
# Copier ce fichier vers .env et remplir les valeurs

# ===========================================
# AUTHENTIFICATION APPLICATION (obligatoire)
# ===========================================
# Identifiants pour accéder à l'interface web
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme
# Clé secrète pour les tokens JWT (générer une clé aléatoire)
# Exemple: python -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET_KEY=votre_cle_secrete_a_changer_en_production

# ===========================================
# IDENTIFIANTS FFE (obligatoire)
# ===========================================
FFE_USERNAME=votre_email@example.com
FFE_PASSWORD=votre_mot_de_passe

# ===========================================
# TELEGRAM (obligatoire)
# ===========================================
# Créer un bot via @BotFather sur Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrSTUvwxYZ
# Obtenir votre chat_id via @userinfobot
TELEGRAM_CHAT_ID=987654321

# ===========================================
# APPLICATION (optionnel)
# ===========================================
# Intervalle entre chaque vérification (secondes)
CHECK_INTERVAL=5

# Niveau de log: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Chemins (par défaut dans ./data/)
DATABASE_PATH=data/engagewatch.db
COOKIES_PATH=data/cookies.json

# ===========================================
# EMAIL (optionnel)
# ===========================================
# Activer les notifications par email
EMAIL_ENABLED=false

# Configuration SMTP
# Pour Gmail: smtp.gmail.com, port 587
# Pour Outlook: smtp.office365.com, port 587
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_SMTP_USE_TLS=true

# Identifiants SMTP
# Pour Gmail, utiliser un "mot de passe d'application":
# https://support.google.com/accounts/answer/185833
EMAIL_SMTP_USERNAME=votre_email@gmail.com
EMAIL_SMTP_PASSWORD=votre_mot_de_passe_application

# Adresses email
EMAIL_FROM=votre_email@gmail.com
EMAIL_TO=destinataire@example.com
